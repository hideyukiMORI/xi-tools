# 日報 2026-09-03 — xi-tools

> 担当: Claude Code（xi-tools セッション・設計リナ）/ 成果: コードの変更なし。候補の判断 1 件（PR #14）と、施主の C# リポの所見

## 今日やったこと

**1. 次の道具の相談 → 作らないと決めた**

施主「Rust を使ってツールを作るの何かいいアイディア無い？ WSL の CLI で二画面ファイラとか？」

| 案 | 判断 | 理由 |
| --- | --- | --- |
| WSL の二画面ファイラ TUI | ✗ 作らない | yazi / broot / joshuto / xplr が既に強い。問題設定が「ファイラが欲しい」で、実測で示せる穴が無い。`ratatui` で数十 crate 入り「本数より 1 本の完成度」に反する |
| C# / WinUI 3 の二画面ファイラを Rust で二重に作る | ✗ 作らない | 施主の目的は「キーボードから手を離さない操作感」で、それは C# 側で正しく進んでいる |
| 2 ディレクトリの差分（`diff -rq` の置き換え） | ⏸ **待ち（穴が無い）** | 施主に「実際に見比べたい 2 ディレクトリ」を聞いたが思い当たらず。穴が開いたら `diff -rq` の所要時間を測ってから。候補一覧に追記（PR #14・`d972bb3`） |

🔑 `scopegrep` と `fleet-top` の着手条件（実際に踏んだ穴・手元で実測できる・1 本で閉じる）を自分に適用した。
**穴が無いところに道具を置くと README に書ける数字が無く、「実測で示す」というリポの性格を自分で崩す。**
起点になりうるのは、C# ファイラで面倒だった操作・Dropbox 片付けの同名新旧・フリートで「遅いから打たれない」コマンドが見つかった時。

**2. 施主の C# リポ `NeNeComander`（`C:\Users\info\WORKS\`）を読んで所見を返した**（読むだけ・変更なし）

一言: **xi-tools と同じ思想（ゲートは機械・ADR・発火の証明）を C# / WinUI 3 に移した、統治が先に完成しているリポ。**

| 観点 | 見たこと |
| --- | --- |
| 規模 | C# 4,650 行（半分がテスト・TestMethod 73）・規範文書 15・ADR 8・スクリプト 9 |
| 良い | パスが型（Windows / UNC / WSL を別 record・解析 1 回・閉じた結果型）。ペインは不変 state ＋ reducer。キー入力は 1 つの mapper（`gg` の 750 ms も注入した時計）。ゲートは 1 本・否定的証明・SHA 固定 Actions・lock file・NuGet 監査 low・変異テスト break 90。ADR 0008 は閾値を下げずに境界を定義 |
| 🔴 重い順の懸念 | ① **コミット 0**（全ファイル未追跡・GitHub の public リポは既定ブランチ無し）。`PROJECT_STATE.md` の語る履歴が git に無く、`deep-review-final.json` は `uncommitted` / `policy-foundation` 段階のもの ② 統治に対して製品が薄い（ListView 2 つとも空・ディレクトリを読む provider 無し） ③ Stryker は `check.ps1` に無く、自分の規約「check.ps1 と CI で実行されないゲートを active と言わない」との整合を要確認 ④ フォルダ名 `NeNeComander`（m が 1 つ）とソリューション名の綴り違い |
| 勧めた次の一手 | commit → push → windows-latest の CI 緑（ゲートが動く証明）→ 縦切り「Windows provider で 1 ディレクトリを読み左ペインに出す」 |
| こちらの限界 | .NET 10 / WinUI は WSL からビルドできない。ゲートは Windows 側のエージェントが回す。こちらは読む・設計相談・規約の突き合わせ |

## 判断の要点

- 「もう一本」を本数のために作らない。2 本とも 0.1.1 まで出て、**xi-tools の次の 1 手は無い**まま
- 他リポの所見は「公称と実態のずれ」を最初に見る（コミット 0 と証跡の段階のずれ）。施主が最も嫌う形なので、褒める前にそこを言う

## 保留

- `NeNeComander` の commit / push は施主側。CI が緑になったら、xi-tools の規約（`docs/coding-rules.md`）と `docs/CODING_RULES.md` の突き合わせを申し出る
