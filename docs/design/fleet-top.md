# fleet-top 設計メモ

**状態: 未着手（2026-09-02）。最初の 1 手は試作による実測。**

## 解く問題

数十の git リポジトリを並べて作業していると、「今どの枝にいて、未コミットがあり、
リモートとどれだけずれ、open な PR と CI の状態がどうか」を**全リポについて**知りたい場面が
1 日に何度もある。それは毎回その場で書き捨てのループになり、**遅い**。

実測（2026-09-01・統合リナ）:

| | 数 |
| --- | --- |
| git リポ | 59 |
| ローカルの状態を直列で（`git status` 等） | 1.5 秒 |
| `gh api` 1 本 | 0.67 秒 |
| `gh api` を直列で 5 本 | 3.4 秒 |
| フリート 42 リポ × 3 種（repo 設定 / open PR / CI 状態）＝ 126 本 | **≒ 84 秒**（外挿） |

🔴 **84 秒かかるコマンドは打たれない。** 打たれないので、見えるはずのもの（期限切れ・4 日間 0 艦の監査）が見えない。
道具の目的は「速くて気持ちいい」ではなく、**打たれるコマンドと打たれないコマンドの境界を越えること**。

## 最初の 1 手: 試作で測る（1 時間）

作る前に、**並列化で本当に境界を越えるか**を数字にする。

1. 使い捨ての Rust プログラム（スクラッチパッド。リポに入れない）で、`gh api` を N 並列（8 / 16 / 32）で叩き、
   126 本の壁時計時間を測る。GitHub API のレート制限（secondary rate limit）に当たるかも見る
2. ローカル側（`git status` / `rev-list --left-right` / `worktree list`）を同じく並列で測る
3. 結果を本メモの「実測」節に書く。**3 秒を切らなければ設計を変える**（キャッシュ・差分取得・GraphQL 1 本化）

## 未決事項

| | 論点 | 選択肢 |
| --- | --- | --- |
| F-1 | 表示 | TUI（`ratatui`・常駐・自動更新）／1 回出力して終わる CLI（`--watch` で繰り返し）／両方 |
| F-2 | GitHub の取り方 | `gh` をサブプロセスで叩く（認証を借りられる・依存が軽い）／`octocrab` 等の API クライアント（依存が重い・token 管理が要る） |
| F-3 | 並行実行 | `tokio`／`std::thread` + チャネル（依存 0 で足りるか） |
| F-4 | 対象の決め方 | 引数のディレクトリ直下を走査／設定ファイル／`--depth` |
| F-5 | 出力の決定性 | 表示順はパス順で固定。API の失敗は「不明」として出し、黙って空にしない（`scopegrep` の終了コード 2 と同じ思想） |

🔴 **F-3 は依存の話であり、ADR が要る**（ARC-004・前例は ADR 0002）。`tokio` + `ratatui` で数十 crate 入る。
`scopegrep` の「既定ビルドの依存 0」はあのツールの性質であって workspace の規則ではないが、**依存を足す理由と数は ADR に書く**。

## 規約から来る制約

| 規則 | 影響 |
| --- | --- |
| ARC-001 | 1 ツール = 1 クレート。`scopegrep-core` とコードを共有しない（共有したくなったら ADR） |
| ARC-003 | 中核（状態を集めて表にする部分）を `no_std` にできるか。I/O が本体の道具なので、できる範囲は狭い。**できない理由を書く** |
| RS-016 | 反復順は決定的。並列で返ってきた結果はパス順に並べ直す |
| RS-014 / RS-015 | 出力は 1 箇所、環境に触るのは配線点だけ。TUI でも同じ |
| QLT-009 | 「速い」は測ってから書く |

## 非目標

- GitHub 以外（GitLab 等）
- リポの操作（fetch・checkout・merge）。**見るだけ**
- フリート固有の概念（kit・weekly-audit）を道具に持ち込まない。汎用の「リポの状態」だけ

## 却下した案

（試作の後で埋める）

## 実測

（試作の後で埋める。想定で埋めない）
