# ツール候補（作らないと決めたものも含む）

> 2026-09-01 に統合リナ（`/home/xi/docker` のセッション）が挙げ、2026-09-02 に xi-tools へ写した。
> **候補は「面白そう」では採らない。** 採用条件は `scopegrep` と同じ 3 つ:
> 実際に踏んだ穴が問題設定になっている／手元の実データで実測できる／1 本で問題が閉じる。

| 候補 | 何を解くか | 状態 | 判断 |
| --- | --- | --- | --- |
| **`scopegrep`** | grep が返さない「ヒットが構造のどこに属するか」 | ✅ **0.1.0 公開済み**（2026-09-02） | 完了。機能追加は困った人が現れてから |
| **`fleet-top`** | 数十リポの状態（枝・未コミット・ahead/behind・open PR・CI・古い枝）を 1 画面で | ✅ **0.1.0 公開済み**（2026-09-02）。設計メモ [`fleet-top.md`](fleet-top.md)・[ADR 0003](../adr/0003-fleet-top-fetches-github-via-chunked-graphql.md) | 試作の実測で境界（3 s）を越えたので作った。実機 60 リポ 1.6〜1.8 s |
| `recall-flat` | 正規化済みベクトルの SIMD 総当たり。`nene-recall` の比較用ストア | ⏸ 待ち | **xi-tools には置かない**（ツールではなくシステムの構成要素。置き場は `nene-recall/rust/`）。Recall の Phase 1 の 7 番の直前まで 1 行も書かない |
| `recall-bench` | Recall の p95 測定（測定側に GC が無いことが利点） | ✗ 消えた | recall レーンが Go で評価ハーネス（ADR 0013）を作った。要件が p95 で GC ノイズは統計に埋まる |
| `board` TUI（ratatui） | 板の常駐 TUI を Python から置き換え | ✗ 保留 | 動いているものの書き直し。必要性が低い |
| `chat-relay` 受信口 | MCP 未接続でも 200 ok を返す黒穴を「接続時だけ ok」に | ✗ 保留 | 実害の記録つきで設計に迷いは無いが、リナ間通信の基盤側の話。xi-tools の第一目的（Rust の実績を示す）には遠い |

## `fleet-top` を次にする理由

- **実際に踏んだ穴**: 2026-09-01 に統合リナが「どの艦が kit に載っているか」「stale な枝は」を何度も手で回した。
  `gh api` は直列で 1 本 0.67 秒（実測）。フリート分（42 リポ × 3 種）で約 84 秒。
  **84 秒かかるコマンドは打たれない**。weekly-audit が 4 日間 0 艦だったのも「見るのが高いから誰も見なかった」から
- **実測できる**: 手元に 59 の git リポがある。並列化の効果は試作 1 本で数字になる
- **1 本で閉じる**: 「艦隊の状態を 1 画面で」は問題として明快

⚠️ **「並列化で 3 秒」は想定であって実測ではない。** 試作で測るまで README に書かない。

## 候補を足すとき

この表に 1 行足し、状態を `🔲` にする前に上の 3 条件を満たすか書く。
「面白い」「Rust らしい」は理由にならない（第一目的は実績を示すことで、道具の有用性は第二目的。CLAUDE.md）。
