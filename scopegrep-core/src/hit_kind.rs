//! ヒットが値の中にあったか、コメントの中にあったか。

/// ヒットの種別。
///
/// 🔑 この道具の存在理由は「**コメント内の一致を設定値と区別できる**」ことである。
/// 区別した結果を捨てずに返すのがこの型の役目で、bool では表さない（RS-002）。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HitKind {
    /// 設定値の中の一致。
    Value,
    /// コメントの中の一致。
    Comment,
}
